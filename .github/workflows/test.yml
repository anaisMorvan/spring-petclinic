name: "Exemple de workflow"
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  greet-user:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Dire bonjour
      uses: anaisMorvan/hello-custom-github-action@main # Utilise l'action locale
      with:
        username: "Jean"
        greeting: "Salut"